package edu.hkcc.pacmanrobot.gamereporter

import java.util.Scanner

import edu.hkcc.pacmanrobot.utils.studentrobot.code.DeviceInfo

/**
 * Created by Winner on 5/4/2015.
 */
class GameReporter {
 /* val scanner = new Scanner(System.in)
  if (deviceInfoMessengers.name.equels(DeviceInfo.DEVICE_TYPE_UNCLASSED_ROBOT)) {
    val input: Int = scanner.nextInt()
    var classed: Boolean = false
    while (!classed)
      printf("%s is unclassed robot, please enter the type of robot. 1:Student robot, 2:Deadline robot, 3:Assignment robot ", deviceInfoMessengers.getInetAddress.getHostName)
    deviceType = input
    if (input == 1) {
      outputStream.writeObject(DeviceInfo.DEVICE_TYPE_STUDENT_ROBOT)
      classed = true
    }
    else if (input == 2) {
      outputStream.writeObject(DeviceInfo.DEVICE_TYPE_DEADLINE_ROBOT)
      classed = true
    }
    else if (input == 3) {
      outputStream.writeObject(DeviceInfo.DEVICE_TYPE_ASSIGNMENT_ROBOT)
      classed = true
    }
    else println("Please enter number 1 to 3")
  }*/

}
